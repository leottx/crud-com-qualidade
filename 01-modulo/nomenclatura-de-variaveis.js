var n1 = 2;
var n2 = 3;

console.log(n1 + n2);

// Como devs, o que mais fazemos é LER código!
// Contexto é extremamente importante!
const userFirstInputNumber = 10; // Especificando o tipo de dado no nome da variável induz a compreensão do resultado que deve ser esperado, no caso, um number.
const userSecondInputNumber = 10;

console.log(userFirstInputNumber + userSecondInputNumber);